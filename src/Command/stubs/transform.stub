<?php

namespace {%namespace%};

use Jian1098\TpRepository\Command\Transform;

class {%className%} extends Transform
{
    public function transform($items)
    {
        return [
            'id'            => $items['id'],
            'createTime'    => $items['create_time'],
            'updateTime'    => $items['update_time'],
        ];
    }
}